// Interface Energia
// Define a estrutura básica para classes que lidam com cálculos de energia
export default interface Energia {
    preco: number; // O preço total da energia consumida
    tarifa: number; // A tarifa aplicada para o cálculo
}